<div class="opened-files">
    <div class="files-tabs <%if(ctx.interfaceState === 'edit'){%>sortable<%}%>" id="files-tabs">
        <%ctx.openedFiles.forEach((file) => {%>
        <div class="file-tab <%if((ctx.selectedFile) && (ctx.selectedFile.id === file.id)){%>selected<%}%>" onclick="changeFileOnClick('<%=file.id%>', this)">
            <div class="file-name"><%=file.name%>.<%=file.type%></div>
            <div class="buttons">
                <%if (ctx.interfaceState === 'edit') {%>
                <div class="button" onclick="event.stopPropagation();closeFileOnClick('<%=file.id%>', this);">
                    <div class="icon gray-4"><%-Icons.render('x')%></div>
                </div>
                <%}%>
            </div>
        </div>
        <%});%>
    </div>
</div>